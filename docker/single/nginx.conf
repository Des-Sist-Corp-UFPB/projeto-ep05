server {
    listen 8080;
    server_name _;

    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # ── Backend (Spring Boot roda no mesmo container, em 127.0.0.1:8081) ──────
    # OBS: o painel de deploy da disciplina encaminha a porta pública fixa
    # (8105) pra porta 8080 do container, e esse campo não é editável por nós.
    # Por isso o Nginx passou a escutar na 8080, e o Java foi pra 8081.
    location ^~ /api/      { proxy_pass http://127.0.0.1:8081; }
    location ^~ /ping      { proxy_pass http://127.0.0.1:8081; }
    location ^~ /actuator/ { proxy_pass http://127.0.0.1:8081; }
    location ^~ /webjars/  { proxy_pass http://127.0.0.1:8081; }
    location ^~ /images/   { proxy_pass http://127.0.0.1:8081; }
    location ^~ /admin     { proxy_pass http://127.0.0.1:8081; }
    location ^~ /sysadmin  { proxy_pass http://127.0.0.1:8081; }
    location ^~ /produtos  { proxy_pass http://127.0.0.1:8081; }
    location ^~ /logout    { proxy_pass http://127.0.0.1:8081; }
    location ^~ /css/      { proxy_pass http://127.0.0.1:8081; }
    location ^~ /js/       { proxy_pass http://127.0.0.1:8081; }

    # ── Assets do Vite → React (arquivos hasheados, cache longo) ──────────────
    location ^~ /assets/ {
        root /usr/share/nginx/html;
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # ── React SPA (fallback para index.html nas rotas do React Router) ────────
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
